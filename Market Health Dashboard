st.title("Market Health Dashboard")

col1, col2, col3 = st.columns(3)
col1.metric("Stability Score", f"{market_stability_score(df):.2f}")
col2.metric("24h Return",      f"{df['close'].pct_change(24).iloc[-1]:.2%}")
col3.metric("Volatility",      f"{df['vol'].tail(200).mean():.4f}")

st.line_chart(df[["close", "vol"]].tail(300))
